# Factorio Helper

## Commands

Add item to the currently opened inventory (space platforms)

```lua
/c game.player.print(game.player.opened.get_inventory(1).insert{name="firearm-magazine", count=100})
```
